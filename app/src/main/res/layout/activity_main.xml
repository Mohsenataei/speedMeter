<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:background="@drawable/navy_blue_background"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.mohsen.speedmeter.activity.MainActivity">





    <fragment
        android:id="@+id/mainFragment"
        android:name="androidx.navigation.fragment.NavHostFragment"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:background="@drawable/navy_blue_background"
        app:defaultNavHost="true"
        app:navGraph="@navigation/nav_graph"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_marginBottom="60dp"
        app:layout_constraintTop_toTopOf="parent"/>

   <!-- <co.avalinejad.iq.component.CustomCurvedBottomNavigationView
        android:id="@+id/mainBottomNavigation"
        android:layout_width="match_parent"
        android:layout_height="80dp"
        android:background="@null"
        android:clickable="true"
        android:clipToPadding="false"
        android:layoutDirection="ltr"
        android:layout_gravity="bottom"
        android:paddingTop="20dp"
        app:itemIconTint="@color/textDark"
        app:itemTextColor="@color/textDark"
        app:layout_constraintBottom_toBottomOf="parent"
        app:menu="@menu/menu_bottom_nav" />-->

    <FrameLayout android:id="@+id/container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/navy_blue_background"
        android:translationZ="200dp"
        android:visibility="gone"/>

</androidx.constraintlayout.widget.ConstraintLayout>